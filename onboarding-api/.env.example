DATABASE_URL="postgresql://onboarding:onboarding@localhost:5433/onboarding?schema=public"
PORT=3000
NODE_ENV=development
JWT_SECRET="dev-only-change-me"
# Comma-separated list of sandbox API keys created by `npm run seed` is printed on seed.
DOCUMENT_STORAGE_DIR="./.storage"
WEBHOOK_TIMEOUT_MS=5000
RATE_LIMIT_PER_MINUTE=1000
